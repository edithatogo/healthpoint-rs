# Synthetic HTTP examples

These files are copies of the synthetic testkit FHIR resources arranged as HTTP response examples. They are not Healthpoint-origin data and must stay synthetic.

Use them with `bin/mock-healthpoint-server` for offline client and MCP contract smoke tests.
